# TODO

- Singple Page Application (SPA) with GUI for creating and editing JSON Schema files for use with ChatAndReact chat interface.  It should be able to create new Schemas with pages of questions/inputs as well as branching logic.  It should also be able to import existing Schemas and allow for editing/saving of them.  It should then allow for exporting the JSON Schema from the browser.